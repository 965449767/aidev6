#!/bin/sh
# gradlew — AIDev 构建入口包装器：透明调用 aidev-build-request
# 真实 Gradle 包装器在 gradlew.real
# 用法: ./gradlew [Gradle 参数...]

exec /usr/local/bin/aidev-build-request --project "$(cd "$(dirname "$0")" && pwd)" "$@"
