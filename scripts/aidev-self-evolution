#!/bin/bash
# aidev-self-evolution — 已废弃（自 2026-07 起重构）
#
# 旧模型：本脚本后台扫描 .aidev-loop/build-failure-*.json，自动调 opencode 改码并触发
# BuildBridgeService 偷偷重建。该"后台偷跑"模型已被废弃，原因是：
#   - OpenCode 看不到实时构建输出，只能去 cat 一份会过期的失败日志，常读到旧日志卡死；
#   - 手动按钮与后台守护两套入口逻辑重复、难以排错。
#
# 新模型（OpenCode 自驱）：
#   1) 手动：ServerPanel 按钮把「本次失败的完整日志路径」交给终端 OpenCode；
#   2) 自驱：OpenCode 直接跑 `aidev-build`，在终端看实时输出，失败就读对应日志改码，再跑，循环；
#   3) 验证 = 构建通过 + 安装启动 + 运行不崩（`aidev-build` 默认一条龙部署，
#      --no-deploy 可关；启动后查 logs/<pkg>/crash.log 或跑 aidev-crash-report）。
#
# 因此本脚本不再自动重建。如需手动触发一次修复，请在终端直接用 OpenCode（见上方）。

echo "aidev-self-evolution 已废弃：构建失败修复现在由 OpenCode 自驱（终端跑 aidev-build 验证）。"
echo "详见 ServerPanel 按钮说明与 workspace AGENTS.md。"
exit 0
